{
  "name": "ts_template",
  "version": "0.0.1",
  "description": "Template for typescript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "LaCuevaDelCodigo",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "linter:check": "ts-standard",
    "linter:fix": "ts-standard --fix",
    "test": "npx jest test",
    "test:coverage": "npx jest test --coverage",
    "test:watch": "npx jest test --watchAll",
    "test:watch:coverage": "npx jest test --watchAll --coverage"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">= 18.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.2.2",
    "@types/node": "^18.11.9",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3",
    "ts-standard": "^12.0.2",
    "typescript": "^4.8.4"
  }
}